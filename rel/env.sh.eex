<%# TODO: patch this properly without manually altering the file %>
<%# ip=$(grep fly-local-6pn /etc/hosts | cut -f 1) %>
ip=127.0.0.1
export RELEASE_DISTRIBUTION=name
export RELEASE_NODE=$FLY_APP_NAME@$ip
export LIVE_BEATS_SERVER_IP=$ip
